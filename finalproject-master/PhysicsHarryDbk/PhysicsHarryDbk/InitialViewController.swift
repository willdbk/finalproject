//
//  ViewController.swift
//  PhysicsHarryDbk
//
//  Created by Mobile on 4/26/16.
//  Copyright © 2016 com.dbk.coding. All rights reserved.
//

import UIKit

class InitialViewController: UIViewController {

    override func viewDidLoad() {
        /*
        super.viewDidLoad()
        let titleLabel = UILabel(frame: CGRectMake(0,0,201,23))
        titleLabel.text = "Physics App"
        titleLabel.center = CGPointMake(self.view.bounds.size.width/2, self.view.bounds.size.height*1/3)
        titleLabel.textAlignment = NSTextAlignment.Center
        self.view.addSubview(titleLabel)
        let startButton = UIButton(frame: CGRectMake(0,0,201,23))
        startButton.center = CGPointMake(self.view.bounds.size.width/2, self.view.bounds.size.height*2/3)
        startButton.setTitle("Start App", forState: UIControlState.Normal)
        self.view.addSubview(startButton)
        */
        
        // Do any additional setup after loading the view, typically from a nib.
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }


}

